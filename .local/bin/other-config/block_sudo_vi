#!/bin/sh
string="vi vim nvim"
substring="$1"
if test "${string#*$substring}" != "$string"; then
    echo "Don't run vi with sudo as it will create a stray \$XDG_CONFIG_HOME folder"
    echo "Use sudovi instead."
    exit
fi

/usr/bin/sudo "$@"
